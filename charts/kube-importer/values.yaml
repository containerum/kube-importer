replicaCount: 1

image:
  repository: containerum/kube-importer
  tag: v1.0.0
  pullPolicy: Always
  secret:

service:
  port: 1666
  targetPort: 1666
  externalIP:

rbac:
  serviceAccountName: null

env:
  CH_KUBE_IMPORTER_DEBUG: "true"
  CH_KUBE_IMPORTER_TEXTLOG: "true"
